% =====================================================================================
% PREÁMBULO — se carga desde main.tex con \input, DESPUÉS de estilo_unir-1.sty
%
% ⚠️ NO añadir aquí paquetes que ya carga estilo_unir-1.sty:
%      babel · inputenc · amsmath · amssymb · amsthm · amsfonts · mathabx
%      graphicx · color · colortbl · hyperref · vmargin · fancyhdr · titling · titlesec
%
% ⚠️ NO usar `geometry`: el estilo institucional fija los márgenes con `vmargin` y los dos
%    paquetes son incompatibles. Los márgenes de la UNIR no se tocan.
%
% ⚠️ Se carga con \input y NO con \include: \include fuerza un salto de página y es ilegal
%    dentro del preámbulo.
% =====================================================================================

% --- Matemáticas para notación cuántica -------------------------------------------
% `physics` da \bra{}, \ket{}, \braket{}, \Tr{}, etc.
% El \let de abajo es OBLIGATORIO y va antes: `mathabx` (cargado por el estilo de la UNIR)
% define \rank, y `physics` lo define otra vez. Sin liberarlo, la compilación falla.
\let\rank\undefined
\usepackage{physics}
\usepackage{bm}          % negrita en símbolos matemáticos

% --- Tablas y espaciado -----------------------------------------------------------
\usepackage{booktabs}    % \toprule, \midrule, \bottomrule
\usepackage{setspace}
\onehalfspacing

% --- Bibliografía -----------------------------------------------------------------
\usepackage{apacite}

% --- Código -----------------------------------------------------------------------
\usepackage{listings}
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegris}{rgb}{0.5,0.5,0.5}
\lstset{
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegris},
    stringstyle=\color{purple},
    basicstyle=\ttfamily\small,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    showspaces=false,
    showstringspaces=false,
    language=Python
}

% --- Enlaces ----------------------------------------------------------------------
% `hyperref` ya viene del estilo institucional; aquí solo se configura su aspecto.
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    filecolor=magenta,
    urlcolor=blue,
    citecolor=black
}

% --- Numeración por capítulo ------------------------------------------------------
\numberwithin{equation}{chapter}
\numberwithin{figure}{chapter}
\numberwithin{table}{chapter}

% --- Rutas de figuras -------------------------------------------------------------
% Las figuras del proyecto se generan en TFM-Quantum/figures/ y se copian a figuras/.
\graphicspath{{figuras/}{doc/imgs/}}
